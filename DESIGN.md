# calc-site 設計メモ

## サイトのビジョン

単なる「計算できるサイト」ではなく、**株式投資をこれから始める初心者を手助けするサイト**として位置づける。計算ツールはあくまで入り口の一つであり、初心者が抱える不安（NISAって何？どの証券会社を選べばいい？何から始めればいい？）に答えるコンテンツと組み合わせて、証券口座開設などのアクションへ自然に導くことがサイトの目的。

収益源は **A8.net** 経由のアフィリエイト広告。当面は証券会社の口座開設案件（SBI証券・楽天証券など）を軸にする。

## 現在のステータス

- A8.netへ一度提携申請したが**審査に落ちた**（理由は通知なし・不明）
- 方針: すぐに再申請するのではなく、**サイトを改善してから改めて申請する**
- 複数セッション（別デバイス含む）で並行してサイト改善を進めており、本ドキュメントはそれらをマージした後の実態を反映したもの

## 収益目標

- **目標: 月10万円**
- 軸となる案件: 証券会社の口座開設（A8.netの成果報酬型）
- 現状のアクセスはほぼゼロのため、まずは検索流入（SEO）による集客基盤づくりが前提となる

### ざっくりの逆算（要検証・目安）

証券口座開設案件の成果報酬は案件・ティアによって幅があるため、以下は計画段階の仮置きの試算。実際にA8.netで案件提携後、承認単価とEPC（クリックあたり収益）を見て随時更新すること。

| 前提（仮） | 値 |
| --- | --- |
| 成果報酬単価 | 1件あたり 1,000〜3,000円 |
| 目標達成に必要な成約数 | 単価1,500円と仮定 → 月67件 |
| CTAクリック率（記事/ツール→広告） | 5〜10% |
| クリック→口座開設 成約率 | 1〜3% |
| 逆算した必要月間セッション数 | 概算 1万〜2万セッション/月 |

→ ゼロから月1〜2万セッションを狙うにはSEO記事の継続的な蓄積が必須。計算ツール単体の指名検索だけでは目標達成は難しく、**記事コンテンツによる検索流入の拡大が最優先課題**になる。

## ターゲットユーザー

- 投資経験がほぼない、またはこれから始めようとしている初心者
- 「NISA」「証券口座」「積立」などの言葉は聞いたことがあるが、具体的な始め方が分からない層
- 検索エンジン経由で「〇〇とは」「〇〇 やり方」等で流入してくることを想定

## コンテンツ戦略

計算ツールに加えて、以下2種類のコンテンツを追加していく方針。

1. **初心者向け解説記事（ブログ形式）**
   - 例: 「NISAとは？」「業種・セクターの選び方」
   - SEOでの検索流入を狙う主力コンテンツ
   - 記事内から関連する計算ツール・証券会社CTAへ内部リンク
2. **始め方ガイド／チェックリスト**
   - 例: 「投資デビューまでの5ステップ」
   - 行動を後押しするコンテンツで、CTAとの親和性が高い

**コンテンツ作成方針:** 他の個人投資家・ブロガー（特定の個人が発信する銘柄選定や分析など）の内容を、本人の許諾なく転載・紹介することはしない。個別銘柄の推奨も行わず、業種・セクター単位や制度の仕組みなど、著作権に抵触しないオリジナルの一般解説にとどめる。

計算ツールは「解説記事やガイドを読んだ人が実際に試す場」として位置づけ、記事・ガイド・ツールを内部リンクで回遊させる導線を設計する。`index.html` では計算ツールより前に記事・ガイドへの導線カードを配置している。

## サイト構成（現状）

| ファイル | 役割 |
| --- | --- |
| `index.html` | トップページ。記事・ガイド・各計算ツール・比較記事へのカード一覧 |
| `compound.html` | 複利計算ツール（元本・年利・年数） |
| `monthly.html` | 積立シミュレーター（毎月積立額・年利・年数、元本と運用益を分けて表示） |
| `fire.html` | FIRE計算ツール（年間支出・想定利回り、Chart.jsによる資産推移グラフ付き） |
| `compare.html` | SBI証券 vs 楽天証券の比較記事＋CTA |
| `about.html` | 運営者情報ページ（サイトの説明・運営目的・免責事項） |
| `privacy.html` | プライバシーポリシー |
| `contact.html` | お問い合わせページ（連絡先メールアドレス掲載） |
| `articles/nisa.html` | 解説記事「NISAとは？」。新NISAの2つの投資枠・メリット・始め方を解説 |
| `articles/sector-guide.html` | 解説記事「長期投資で注目したい業種・セクターの選び方」。業種ごとの特徴とリスクを解説 |
| `guide/getting-started.html` | 始め方ガイド「投資デビューまでの5ステップ」。証券会社選び〜積立開始までの流れ |

すべてのページで `style.css` を共通利用。

- **ヘッダー**: 全ページに `<header class="site-header">`（ロゴ＋ホーム/複利/積立/FIRE/比較へのnav）を各HTMLファイルへ直接記述（インライン）している。過去に `header.html` を fetch で共通読み込みする方式を試したが、GitHub Pages上でパスの問題が起きたため静的なインライン方式に統一した経緯がある（共通include用のファイルは使っていないので、ヘッダーを変更する際は全ページを個別に編集する必要がある）
- **フッター**: 全ページに `<footer class="footer">`（運営者情報／プライバシーポリシー／お問い合わせへのリンク）を設置

## 共通デザインシステム（style.css）

- `.container`: 最大幅900px、中央寄せのページラッパー
- `.card`: 白背景・角丸14px・シャドウ・hoverで浮き上がるアニメーション。各ページはカードの積み重ねで構成される
- `.site-header` / `.header-inner` / `.logo` / `.nav`: 黒背景の共通ヘッダー
- `.footer`: 共通フッター（`.site-footer` という旧クラスは廃止し `.footer` に統一済み）
- `input` / `button` / `label`: フォーム要素の共通スタイル
- `.result`: 計算結果の表示エリア（薄グレー背景）
- `.cta`: 黒背景・白文字の強調ボタン。「収益の核」とコメントされている通り、アフィリエイト導線の要
- `.compare-table`: 比較表用のテーブルスタイル
- `.push-text` / `.note` / `.note-small`: 補足テキスト用のスタイル
- 600px以下でレスポンシブ対応（余白・フォントサイズ縮小、ヘッダーnavは折り返し）

`css/` ディレクトリは存在するが中身は空で、現状未使用。

## 計算ページの実装パターン

各計算ツール（compound / monthly / fire）は共通のパターンで実装されている。

1. `.card` 内にフォーム（`input`群 + `計算する`ボタン）
2. ボタンの `onclick="calc()"` でページ内 `<script>` に定義された関数を実行
3. `#result` 要素に計算結果をテキストで挿入
4. 解説カード（用語説明）
5. CTAカード（証券口座開設への誘導）

`calc()` はページごとに個別実装されており、共通化・モジュール化はされていない（コードの重複あり）。`fire.html` のみ Chart.js（CDN読み込み）で資産推移グラフを表示する。

## CTA / マネタイズ導線

- ほとんどのCTAリンクは `href="#"` のダミーのまま（A8.net未提携のため）。`monthly.html` のみ暫定的に `compare.html` への内部リンクに変更済み（`<!-- TODO: A8.net提携後、実際のアフィリエイトURLに差し替える -->` とコメントあり）。他のページも同様に、提携までの間はダミーリンクを避けて内部リンクに揃えるか検討する
- `compound.html` は計算実行後にのみCTAを表示する仕組み（`#cta-after-result`）を持つが、他のページ（monthly/fire）は常時表示のCTAカードのみで、計算後CTAの仕組みは未導入
- CTA文言・配置はページごとに微妙に異なり統一されていない

## A8.net再申請に向けた準備（優先度高）

落選理由は不明。一般的にASP審査で重視されやすい項目のチェックリスト。再申請前にここを潰す。

- [x] **プライバシーポリシーページ** → `privacy.html` 作成済み
- [x] **運営者情報ページ** → `about.html` 作成済み（サイト説明・運営目的・免責事項）
- [x] **お問い合わせページ** → `contact.html` 作成済み（連絡先メールアドレス: calc.side.project@gmail.com）
- [x] 全ページに共通ヘッダー（サイトnav）・共通フッター（about/privacy/contactへのリンク）を設置
- [x] **コンテンツ量・独自性**を増やす → 解説記事2本（NISA解説・セクター選び方）＋始め方ガイド1本を追加。index.htmlにも導線カードを追加
- [ ] **CTAリンクが `href="#"` のダミーのまま**残っている箇所が大半。A8.net提携前の暫定対応（内部リンク化）を全ページに揃えるか検討
- [ ] サイトタイトル・ディスクリプションが「計算サイト」ではなく「初心者向け投資支援サイト」として一貫して伝わる内容になっているか見直す
- [ ] 独自ドメイン・HTTPS配信になっているか確認する

## 未整備・今後の検討事項

- SEO集客のための記事コンテンツを継続的に追加する
- `calc()` ロジックの重複を共通JSファイルに切り出す余地がある
- ヘッダー・フッターがページごとにインライン重複しているため、ビルドツール導入や共通include（GitHub Pages対応の方法で）の再検討余地がある
- 空の `css/` ディレクトリの扱い（削除 or 用途決定）
- 計算後CTA表示の仕組み（`compound.html` にのみ存在）を他ページにも展開するか検討
- アクセス解析（Google Analytics等）を導入し、CTAクリック率・成約率を計測できるようにする
- サイトの改善が一巡したら、A8.netへ再申請する
