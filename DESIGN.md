# calc-site 設計メモ

## サイトのビジョン

単なる「計算できるサイト」ではなく、**株式投資をこれから始める初心者を手助けするサイト**として位置づける。計算ツールはあくまで入り口の一つであり、初心者が抱える不安（NISAって何？どの証券会社を選べばいい？何から始めればいい？）に答えるコンテンツと組み合わせて、証券口座開設などのアクションへ自然に導くことがサイトの目的。

収益源は **A8.net** 経由のアフィリエイト広告。当面は証券会社の口座開設案件（SBI証券・楽天証券など）を軸にする。

## 現在のステータス

- A8.netへ一度提携申請したが**審査に落ちた**（理由は通知なし・不明）
- 方針: すぐに再申請するのではなく、**サイトを改善してから改めて申請する**
- 理由が不明なため、一般的にASP審査で見られやすいポイントを次節のチェックリストとして洗い出し、優先的に潰していく

## 収益目標

- **目標: 月10万円**
- 軸となる案件: 証券会社の口座開設（A8.netの成果報酬型）
- 現状のアクセスはほぼゼロのため、まずは検索流入（SEO）による集客基盤づくりが前提となる

### ざっくりの逆算（要検証・目安）

証券口座開設案件の成果報酬は案件・ティアによって幅があるため、以下は計画段階の仮置きの試算。実際にA8.netで案件提携後、承認単価とEPC（クリックあたり収益）を見て随時更新すること。

| 前提（仮） | 値 |
| --- | --- |
| 成果報酬単価 | 1件あたり 1,000〜3,000円 |
| 目標達成に必要な成約数 | 単価1,500円と仮定 → 月67件 |
| CTAクリック率（記事/ツール→広告） | 5〜10% |
| クリック→口座開設 成約率 | 1〜3% |
| 逆算した必要月間セッション数 | 概算 1万〜2万セッション/月 |

→ ゼロから月1〜2万セッションを狙うにはSEO記事の継続的な蓄積が必須。計算ツール単体の指名検索だけでは目標達成は難しく、**記事コンテンツによる検索流入の拡大が最優先課題**になる。

## ターゲットユーザー

- 投資経験がほぼない、またはこれから始めようとしている初心者
- 「NISA」「証券口座」「積立」などの言葉は聞いたことがあるが、具体的な始め方が分からない層
- 検索エンジン経由で「〇〇とは」「〇〇 やり方」等で流入してくることを想定

## コンテンツ戦略

計算ツールに加えて、以下2種類のコンテンツを追加する。

1. **初心者向け解説記事（ブログ形式）**
   - 例: 「NISAとは？」「証券口座の選び方」「複利とは」
   - SEOでの検索流入を狙う主力コンテンツ
   - 記事内から関連する計算ツール・証券会社CTAへ内部リンク
2. **始め方ガイド／チェックリスト**
   - 例: 「口座開設の手順」「投資デビューまでの5ステップ」
   - 行動を後押しするコンテンツで、CTAとの親和性が高い
   - ステップの最後に証券会社の口座開設CTAを配置

計算ツールは「解説記事やガイドを読んだ人が実際に試す場」として位置づけ、記事・ガイド・ツールを内部リンクで回遊させる導線を設計する。

## サイト構成（現状）

| ファイル | 役割 |
| --- | --- |
| `index.html` | トップページ。各計算ツール・比較記事へのカード一覧とCTA |
| `compound.html` | 複利計算ツール（元本・年利・年数） |
| `monthly.html` | 積立シミュレーター（毎月積立額・年利・年数） |
| `fire.html` | FIRE計算ツール（年間支出・想定利回り） |
| `compare.html` | SBI証券 vs 楽天証券の比較記事＋CTA |
| `about.html` | 運営者情報・お問い合わせページ（運営者名・連絡先は確定次第更新予定） |
| `privacy-policy.html` | プライバシーポリシー |

すべてのページで `style.css` を共通利用。全ページ共通の `.site-footer`（トップ／運営者情報・お問い合わせ／プライバシーポリシーへのリンク）を設置済み。ヘッダー側の共通ナビゲーション（トップに戻る導線など）は未整備。

### コンテンツページ（追加済み）

| ファイル | 役割 |
| --- | --- |
| `articles/nisa.html` | 解説記事「NISAとは？」。新NISAの2つの投資枠・メリット・始め方を解説 |
| `guide/getting-started.html` | 始め方ガイド「投資デビューまでの5ステップ」。証券会社選び〜積立開始までの流れ |

`index.html` の先頭（計算ツールより前）にこの2つへの導線カードを配置。今後、記事・ガイドが増える前提で、カテゴリ別の一覧ページやサイト内ナビゲーションの整備が必要になる。

## 共通デザインシステム（style.css）

- `.container`: 最大幅900px、中央寄せのページラッパー
- `.card`: 白背景・角丸14px・シャドウ・hoverで浮き上がるアニメーション。各ページはカードの積み重ねで構成される
- `input` / `button` / `label`: フォーム要素の共通スタイル
- `.result`: 計算結果の表示エリア（薄グレー背景）
- `.cta`: 黒背景・白文字の強調ボタン。「収益の核」とコメントされている通り、アフィリエイト導線の要
- 600px以下でレスポンシブ対応（余白・フォントサイズ縮小）

`css/` ディレクトリは存在するが中身は空で、現状未使用。

## 計算ページの実装パターン

各計算ツール（compound / monthly / fire）は共通のパターンで実装されている。

1. `.card` 内にフォーム（`input`群 + `計算する`ボタン）
2. ボタンの `onclick="calc()"` でページ内 `<script>` に定義された関数を実行
3. `#result` 要素に計算結果をテキストで挿入
4. 解説カード（用語説明）
5. CTAカード（証券口座開設への誘導、`href="#"` のプレースホルダーリンク）

`calc()` はページごとに個別実装されており、共通化・モジュール化はされていない（コードの重複あり）。

## CTA / マネタイズ導線

- 全ページのCTAは、A8.net提携までの暫定措置として `compare.html`（証券会社比較）または `guide/getting-started.html`（始め方ガイド）への内部リンクにしている。各箇所に `<!-- TODO: A8.net提携後、実際のアフィリエイトURLに差し替える -->` とコメントしてあるので、提携後はこれを目印に実際のアフィリエイトURLへ差し替える
- `compound.html` は計算実行後にのみCTAを表示する仕組み（`#cta-after-result`）を持つが、他のページ（monthly/fire）は常時表示のCTAカードのみで、計算後CTAの仕組みは未導入
- CTA文言・配置はページごとに微妙に異なり統一されていない
- 今後、記事・ガイドコンテンツにもCTAを配置する統一パターンを設計する必要がある

## A8.net再申請に向けた準備（優先度高）

落選理由が不明なため確実ではないが、一般的にASP審査で重視されやすい項目のチェックリスト。再申請前にここを潰す。

- [x] **プライバシーポリシーページ**を設置する → `privacy-policy.html` 作成済み
- [x] **運営者情報／お問い合わせページ**を設置する → `about.html` 作成済み。ただし運営者名（ペンネーム/屋号）・公開用メールアドレスは**未確定でプレースホルダー（「準備中」）のまま**。確定次第、`about.html` の「運営者」「お問い合わせ」セクションを更新する必要あり（A8.net再申請前に必須）
- [x] 全ページ（index/compound/monthly/fire/compare/about/privacy-policy）に共通フッターを追加し、`about.html`・`privacy-policy.html`へ相互リンク
- [x] **コンテンツ量・独自性**を増やす → 解説記事「NISAとは？」(`articles/nisa.html`)、始め方ガイド「投資デビューまでの5ステップ」(`guide/getting-started.html`) を追加。`index.html` にも導線カードを追加し、計算ツールより前に配置（教育コンテンツ→ツールの順の導線に変更）。他ジャンルの記事も継続的に追加していく
- [x] **CTAリンクが `href="#"` のダミーのまま**放置されている状態を解消 → 全ページのCTAを、A8.net提携までの暫定措置として `compare.html`（証券会社比較）や `guide/getting-started.html`（始め方ガイド）への内部リンクに変更。各箇所に `<!-- TODO: A8.net提携後、実際のアフィリエイトURLに差し替える -->` のコメントを残してあるので、提携後はこれらを検索して実際のアフィリエイトURLに置き換える
- [ ] サイト全体の完成度を上げる（ヘッダーナビゲーション、デザインの一貫性）
- [ ] 独自ドメイン・HTTPS配信になっているか確認する
- [ ] サイトタイトル・ディスクリプションが「計算サイト」ではなく「初心者向け投資支援サイト」として一貫して伝わる内容になっているか見直す

## 未整備・今後の検討事項

- SEO集客のための記事コンテンツ（初心者向け解説記事・始め方ガイド）を追加する
- 記事・ガイドが増えることを見越して、共通ヘッダー／フッター・カテゴリナビゲーション・パンくずを整備する
- `calc()` ロジックの重複を共通JSファイルに切り出す余地がある
- 空の `css/` ディレクトリの扱い（削除 or 用途決定）
- 計算後CTA表示の仕組み（`compound.html` にのみ存在）を他ページにも展開するか検討
- アクセス解析（Google Analytics等）を導入し、CTAクリック率・成約率を計測できるようにする
- サイトの改善が一巡したら、A8.netへ再申請する
